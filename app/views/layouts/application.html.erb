<!DOCTYPE html>
<html>
<head>
  <title><%= @metadata.metatitle if @metadata.metatitle != nil %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="title" content="<%= @metadata.metatitle if @metadata.metatitle != nil %>" />
  <meta name="keywords" content="<%= @metadata.keywords if @metadata.keywords != nil %>" />
  <meta name="description" content="<%= @metadata.metadescription if @metadata.metadescription != nil %>" />
  <%= @metadata.headertags.html_safe if @metadata.headertags != nil %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
	<body>
		<div class="container-fluid">
		    <div class="row-fluid">
		    	<div class="span3">
		    		<%= render partial: "sidebar" %>
		    	</div>
		    	<div class="span9">
		    		<% if notice %>
				  		<p class="alert alert-success"><%= notice %></p>
					<% end %>
					<% if alert %>
				  		<p class="alert alert-danger"><%= alert %></p>
					<% end %>
					<%= yield %>
				</div>
			</div>
		</div>
	</body>
</html>
<%= render partial: "footer"%>
