<% @projects.each do |p| %>
	<div class="row-fluid">
		<div class="span12">
			<ul class="thumbnails">
				<% p.each do |id,name,picture| %>
				        <%= render partial: 'projectcard', :locals => {:name => name, :id => id, :picture => picture} %>
				<% end %>
			</ul>
		</div>
	</div>
<% end %>