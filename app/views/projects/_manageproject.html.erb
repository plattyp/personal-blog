<%= form_for(@project) do |f| %>
	<%= f.label "Name" %>
	<%= f.text_field :name %>
	<%= f.label "Github URL" %>
	<%= f.url_field :github_url %><br />
	<%= f.submit :submit, :class => 'btn' %>
<% end %>